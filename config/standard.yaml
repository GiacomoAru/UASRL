# --- CONFIGURAZIONE ESPERIMENTO ---
exp_name: "base+wp"
env_id: "std"
seed: 61045
cuda: true
torch_deterministic: true
total_timesteps: 50000

# --- RETI NEURALI (ARCHITETTURA) ---
# Liste di interi che definiscono i nodi per ogni livello nascosto
actor_network_layers: [128, 128, 128]
q_network_layers: [128, 128, 128]
q_ensemble_n: 5  # Numero di reti Q nell'ensemble

# --- LEARNING RATES (VELOCITÀ DI APPRENDIMENTO) ---
policy_lr: 0.0004
q_lr: 0.0004
alpha_lr: 0.0004

# --- PARAMETRI BUFFER E BATCH ---
buffer_size: 120000
batch_size: 256
bootstrap_batch_proportion: 0.8
learning_starts: 1000  # Passi casuali iniziali

# --- IPERPARAMETRI RL ---
gamma: 0.995           # Discount factor
tau: 0.005             # Soft update per target networks
alpha: 1.0             # Coefficiente entropia iniziale
autotune: true         # Tuning automatico di alpha
noise_clip: 0.5

# --- AGGIORNAMENTI E FREQUENZE ---
update_per_step: 1           # Quanti gradient updates per step ambientale
policy_frequency: 4          # Ogni quanti step aggiornare la policy
target_network_frequency: 1  # Ogni quanti step aggiornare la target net

# --- LOGGING E METRICHE ---
loss_log_interval: 100
metrics_log_interval: 300
metrics_smoothing: 0.985